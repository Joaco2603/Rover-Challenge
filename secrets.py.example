# Copiar a secrets.py en cada placa (no commitear passwords reales).
# Mismo code.py en los dos robots; cambia este archivo.

ARUCO_ID = 10

# Red a la que también está la laptop de visión.
WIFI_SSID = "CAMBIAR"
WIFI_PASSWORD = "CAMBIAR"

# IP de esa laptop en la LAN. Nunca 127.0.0.1: eso es "esta misma máquina".
VISION_HOST = "192.168.1.10"

# MAC del otro rover. El placeholder desactiva ESP-NOW (el loop igual arranca).
PEER_MAC = "AA:BB:CC:DD:EE:FF"
